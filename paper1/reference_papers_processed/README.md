# Processed References

Recommended tracked artifacts:

- `metadata.jsonl`
- `shortlist.jsonl`
- `cards/*.md`
- converted paper Markdown when licensing/policy permits

Raw PDFs belong in `../reference_papers_origin/` and are ignored by Git.
